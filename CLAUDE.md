# CLAUDE.md — Claude Code 向けプロジェクト運用方針

本ファイルは Claude Code で本プロジェクトを操作する際の運用方針です。Codex 向けの詳細方針は `AGENTS.md` を参照してください。方針が競合する場合は `AGENTS.override.md` → `AGENTS.md` → 本ファイルの順で優先されます。

## プロジェクトの本質

- 監査業務・証跡管理（Mirai Audit Workpaper）の**構想・要件・設計文書群**です。実行可能なアプリ・画面・API・DB・テスト結果は含みません。
- 文書の状態は「実装済み／計画／未確定」を厳密に区別し、「計画」を実装済みと誤記しないでください。
- 監査判断、重要度、監査意見、是正完了、法的評価は人が決定します。システム・AIが自動決定する記述を追加しないでください。

## 作業前に必ず実施すること

1. `python3 scripts/check-links.py` を実行し、全リンク（Markdown・HTML）が PASS することを確認する。
2. 変更対象ファイルを読み、文書ID・版・日付・状態区分・用語（`docs/用語集.md`）との整合を確認する。
3. 秘密情報（.env、資格情報、トークン、個人・会社の実データ）が含まれていないことを確認する。

## 変更時のルール

- 既存のユーザー変更と無関係な差分を作らない。原典資料（業務内容一覧・システム要件一覧・システム化アイデア一覧）はリポジトリ直下の複製を編集せず、元ディレクトリからの取り込み手続（コピー＋ハッシュ照合）で反映する。
- 新規文書・用語を追加した場合は、`docs/ドキュメント一覧.md` と `docs/用語集.md` を更新する。
- 未確定事項は推測で確定せず、決定者と期限を `docs/リスク・課題・前提条件一覧.md` へ追記する。
- HTML 文書を変更する場合はタグ構造の整合と印刷表示（`@media print`）を確認する。

## コミット

- main へ直接 push しない。作業 branch で論理単位にコミットし、レビューを経てマージする。
- コミットメッセージは日本語で、変更内容と対象文書IDを簡潔に記載する。

## よくある作業

- リンク検証: `python3 scripts/check-links.py`
- HTML構造検証: 各HTMLのタグ整合チェック（未使用タグ・閉じ忘れの確認）
- 文書追加: docs/ 配下に Markdown を作成し、ドキュメント一覧・README の入口に追加
