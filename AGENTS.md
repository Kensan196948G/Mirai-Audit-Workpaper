# Mirai Audit Workpaper — プロジェクト運用ポリシー（AGENTS.md）

## 0. 適用範囲

本ファイルはリポジトリルートの `AGENTS.md` として配置するプロジェクト単位の Codex 運用ポリシーです。
グローバル設定の運用方針を継承しつつ、本プロジェクト固有の方針を定義します。

## 1. プロジェクト情報

| 項目 | 内容 |
|---|---|
| プロジェクト名 | 監査業務・証跡管理 (Mirai Audit Workpaper) |
| 目的 | 監査役・監査役会が行う年間計画、個別監査、証憑依頼・受領、監査調書、レビュー・承認、指摘、是正・改善確認を、証跡とともに一貫して管理する仕組みの構想・要件・設計文書群を整備する |
| 主な利用者 | 監査役、監査役会、総務部（事務局）、被監査部門、情報システム部門、法務・コンプライアンス |
| 現状 | 構想・要件定義段階。加えて要件定義書 MAW-RD-001 の主要機能を実装したアプリ（app/: Cloudflare Workers + D1、テスト78件PASS・ビルドPASS・CI/CD有効（preview/MVPデプロイ済み・本番は検証済みコードをデプロイ済み））を保有。実データ投入・本番運用は社内決定と受入試験の完了が前提。文書の状態区分（実装済み／計画／未確定）は引き続き厳密に区別する |
| 想定技術構成 | AppSuite（台帳・ワークフロー）、desknet's NEO（通知）、OneDrive（草稿）、DirectCloud（正本）、Entra ID／HENNGE ONE（認証）、Lightblue（AI支援）— すべて候補・未契約 |
| 参照元 | `Mirai-DX-Planning-Resources/01_経営・統治・委員会/` の原典5ファイル（本リポジトリ直下に複製して管理） |

## 2. 言語と対応

- 日本語で対応・解説する。
- 文書・画面・API名は日本語中心、識別子（ID・技術用語）は英語可。

## 3. 文書の状態区分（最重要ルール）

本プロジェクトの文書は「実装済み／計画／未確定」を厳密に区別する。

| 表示 | 意味 | 扱い |
|---|---|---|
| 実装済み | ワークスペースで現物または実行結果を確認できたもの | そのまま維持 |
| 計画 | 要件・構想・運用案として定義したもの。利用可能とは限らない | 「計画」「案」と明記 |
| 未確定 | 社内規程、関係者合意、製品選定、詳細設計等が必要なもの | 決定者・期限を残す |

- 「する」「備える」と記載されていても、要件・設計・運用案であり稼働確認済みとは扱わない。
- 設計記載だけを実装済みと扱わない。実装状況は別管理（未着手／実装中／検証中／受入済み）。
- 監査判断、重要度、監査意見、是正完了、法的評価は人が決定する。AI・システムが自動決定しない。

## 4. 秘密情報・個人情報の保護

- `.env`、資格情報、トークン、秘密鍵、個人・会社の実データを Git・画面・ログ・文書へ出力しない。
- 秘密候補は値を示さず、影響とローテーション方法だけを報告する。
- 通報者情報、要配慮個人情報、未公表情報を未承認の外部サービス（AI含む）へ投入しない。

## 5. 文書の整合性ルール

- 相対リンクは常に実在することを確認する（`python3 scripts/check-links.py` で全リンク検証し PASS を確認）。
- 文書ID・版・作成日はヘッダーの管理情報と本文の参照を一致させる。
- 用語は `docs/用語集.md` に従う。新しい用語を導入したら用語集へ追記する。
- 保存年限、権限、重要度、採用製品など社内決定が必要な事項は「未確定」のままにせず、決定者と期限を `docs/リスク・課題・前提条件一覧.md` または意思決定記録へ追記する。
- 原典資料（業務内容一覧・システム要件一覧・システム化アイデア一覧）はリポジトリ直下の複製を正とし、元ディレクトリの更新は取り込み手続（コピー＋ハッシュ照合）を記録してから反映する。

## 6. Git / リリースルール

- main へ直接 push しない。作業 branch で論理単位に commit する。
- 文書変更はレビュー（内容・リンク・状態区分・整合性）を経てから main へマージする。
- リリースは `docs/変更管理・リリース計画.md` に従う。本番移行は受入試験・権限棚卸し・復元試験・教育の完了が前提。
- app/ のデプロイは preview（mvp-DB）→ production（main-DB）の2段階。production は受入条件クリア後に GitHub Actions の手動実行で行い、実データ投入は社内決定後とする。文書群の「本番デプロイ」は採用製品の導入手続を指す。

## 7. 運用ループ

`Monitor -> Assessment -> Gap/Feature Discovery -> Prioritization -> Development -> Verify -> Review -> Improvement -> Re-assessment` の順で進める。
検証（Verify）とレビュー（Review）を省略しない。小変更は計画立案の手続を最小化してよいが、状態区分とリンク検証は常に実施する。

## 8. STABLE 判定（文書プロジェクト版）

以下をすべて満たした場合のみ STABLE とする。

- 全 Markdown／HTML の相対リンクが解決している（check-links.py PASS）
- HTML のタグ構造にエラーがない
- 文書間の日付・版・用語・状態区分に矛盾がない
- 秘密情報・実データが含まれない
- 未確定事項に決定者と期限が記録されている（決定済みと誤記しない）

## 9. 停止・報告

社内規程・関係者合意・製品選定など人間の承認が必要な決定は、推測で確定させず「未確定」として決定者・期限を記録して次工程へ進む。
法令・契約・個人情報への重大影響が生じる場合は作業を止め、証拠・ブロッカー・解除条件を報告する。
