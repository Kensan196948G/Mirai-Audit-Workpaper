# AGENTS.override.md — プロジェクト固有の上書き方針

本ファイルは Codex のグローバル指示・親リポジトリ方針に対する、本プロジェクト（Mirai Audit Workpaper）固有の上書き方針を定義する。

## 1. 上書きする方針

| 対象 | グローバル／親方針 | 本プロジェクトの上書き方針 |
|---|---|---|
| 実装判定 | コード・テスト・ビルド結果で実装を判定する | 本リポジトリは**文書のみのプロジェクト**。実装は原典資料の「実装済み資料」と、文書の状態区分（実装済み／計画／未確定）で判定する。コード・テスト・CI結果は存在しないため「未確認」として扱う |
| 本番デプロイ | main マージ後、CI 成功で本番デプロイ | 本番稼働システムを含まないため、デプロイは「採用製品の導入手続」を指す。main マージ条件は文書整合性（リンク PASS・状態区分・秘密情報なし）とする |
| CI | 必須チェックとして CI を要求 | リンク検証スクリプト（`scripts/check-links.py`）が CI 相当の検証であり、実行して PASS を確認する |
| 言語 | — | 日本語で対応・解説する（親方針と同一） |

## 2. 上書きしない方針

- 監査判断・重要度・是正完了・法的評価の自動決定は禁止（人が決定する）— 上書きしない。
- 秘密情報・個人情報・実データの Git／文書への出力禁止 — 上書きしない。
- main 直接 push 禁止、作業 branch＋レビュー、論理単位の commit — 上書きしない。
- 既存のユーザー変更と無関係な差分の保護 — 上書きしない。原典資料は複製として取り込み、元ファイルを書き換えない。

## 3. 適用順序

グローバル設定 → 親リポジトリ AGENTS.md → 本ファイル（AGENTS.override.md）→ リポジトリ AGENTS.md の順で方針が適用される。矛盾がある場合は本ファイルの記載を優先する。
