# AGENTS.override.md — プロジェクト固有の上書き方針

本ファイルは Codex のグローバル指示・親リポジトリ方針に対する、本プロジェクト（Mirai Audit Workpaper）固有の上書き方針を定義する。

## 1. 上書きする方針

| 対象 | グローバル／親方針 | 本プロジェクトの上書き方針 |
|---|---|---|
| 実装判定 | コード・テスト・ビルド結果で実装を判定する | 文書の状態区分（実装済み／計画／未確定）に加え、要件定義書 MAW-RD-001 の主要機能を実装した **app/（Cloudflare Workers + D1）** が存在する。app/ の実装判定は型チェック・lint・テスト（78件）・ビルドの PASS で行う。実データ投入・本番運用は社内決定と受入試験の完了が前提 |
| 本番デプロイ | main マージ後、CI 成功で本番デプロイ | app/ のデプロイは preview（mvp-DB）→ production（main-DB）の2段階。production は受入試験・権限棚卸し・復元試験・教育の完了を前提とし、GitHub Actions の手動実行で行う。文書群の main マージ条件は文書整合性（リンク PASS・状態区分・秘密情報なし）とする |
| CI | 必須チェックとして CI を要求 | 文書はリンク検証スクリプト（`scripts/check-links.py`）、app/ は GitHub Actions（.github/workflows/ci.yml）の型チェック・lint・テスト・ビルドを必須とする。リモート追加後に有効化 |
| 言語 | — | 日本語で対応・解説する（親方針と同一） |

## 2. 上書きしない方針

- 監査判断・重要度・是正完了・法的評価の自動決定は禁止（人が決定する）— 上書きしない。
- 秘密情報・個人情報・実データの Git／文書への出力禁止 — 上書きしない。
- main 直接 push 禁止、作業 branch＋レビュー、論理単位の commit — 上書きしない。
- 既存のユーザー変更と無関係な差分の保護 — 上書きしない。原典資料は複製として取り込み、元ファイルを書き換えない。

## 3. 適用順序

グローバル設定 → 親リポジトリ AGENTS.md → 本ファイル（AGENTS.override.md）→ リポジトリ AGENTS.md の順で方針が適用される。矛盾がある場合は本ファイルの記載を優先する。
